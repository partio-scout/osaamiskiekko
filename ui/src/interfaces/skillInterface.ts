export interface skillInterface {
  credits: number;
  best_match: number | string;
  description: { fi: string };
  id: string;
  keywords: string[];
  match_count: number;
  name: { fi: string };
  organization: string;
  tutkinnonosat: string[];
  url: string;
}
